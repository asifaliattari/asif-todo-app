apiVersion: v1
kind: ConfigMap
metadata:
  name: taskflow-config
  namespace: taskflow
data:
  # Frontend configuration
  NEXT_PUBLIC_APP_NAME: "TaskFlow"
  NEXT_PUBLIC_AUTHOR: "Asif Ali AstolixGen"
  NEXT_PUBLIC_HACKATHON: "GIAIC Hackathon 2026"
  NEXT_PUBLIC_API_URL: "http://taskflow-backend:8000"
  
  # Backend configuration
  ENVIRONMENT: "production"
  FRONTEND_URL: "http://taskflow.local"
  BACKEND_API_URL: "http://taskflow-backend:8000"
  OPENAI_MODEL: "gpt-4o-mini"
